import { Check, Circle, CircleDotDashed } from 'lucide-react'
import type { RuntimeInsight } from '../../types/training'

const stateLabels = { NEEDS_DISCOVERY: 'Đang khám phá nhu cầu', PRODUCT_DISCUSSION: 'Trao đổi sản phẩm', PRICE_DISCUSSION: 'Thảo luận về giá', CLOSING: 'Đang chốt bước tiếp theo' }

export function RuntimeInsightPanel({ insight }: { insight: RuntimeInsight }) {
  return <div><div className="rounded-2xl bg-blue-50 p-4"><div className="flex items-center gap-2 text-xs font-extrabold uppercase tracking-[0.13em] text-blue-600"><CircleDotDashed className="h-4 w-4" />Runtime state · Mock</div><p className="mt-2 font-extrabold text-slate-900">{stateLabels[insight.state]}</p><p className="mt-1 text-xs font-semibold text-slate-500">{insight.state}</p></div><div className="mt-6"><div className="flex items-center justify-between text-sm"><span className="font-bold text-slate-800">Tiến độ chủ đề</span><span className="font-extrabold text-blue-600">{insight.completedTopics.length} / {insight.totalTopics}</span></div><div className="mt-2 h-2 overflow-hidden rounded-full bg-slate-100"><div className="h-full rounded-full bg-blue-600 transition-all" style={{ width: `${(insight.completedTopics.length / insight.totalTopics) * 100}%` }} /></div></div><div className="mt-6"><p className="text-xs font-extrabold uppercase tracking-[0.12em] text-slate-400">Đã biết</p><div className="mt-3 space-y-2">{insight.completedTopics.map((item) => <div key={item} className="flex items-center gap-2 text-sm font-semibold text-slate-700"><span className="grid h-5 w-5 place-items-center rounded-full bg-emerald-50 text-emerald-600"><Check className="h-3 w-3" /></span>{item}</div>)}</div></div><div className="mt-6"><p className="text-xs font-extrabold uppercase tracking-[0.12em] text-slate-400">Còn thiếu</p><div className="mt-3 grid grid-cols-2 gap-2">{insight.missingTopics.map((item) => <div key={item} className="flex items-center gap-2 text-sm text-slate-500"><Circle className="h-4 w-4" />{item}</div>)}</div></div><div className="mt-6 flex items-center justify-between rounded-xl border border-slate-200 px-3 py-3"><span className="text-sm font-bold text-slate-700">Trạng thái deal</span><span className="text-xs font-extrabold text-amber-700">{insight.dealState}</span></div><p className="mt-4 text-xs leading-5 text-slate-400">Dữ liệu mô phỏng để minh họa DTO an toàn trong phiên bản V1. Không phải gợi ý coaching.</p></div>
}
